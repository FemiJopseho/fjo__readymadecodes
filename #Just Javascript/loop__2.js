// to count how many uppercase letters are in the string

let counter = 0;
while (counter <= strLength) {
	str[N];
	if (str[N] === str[N].toUpperCase()) {
		howMany += 1;
	}
	N += 1;
	counter += 1;
}
console.log(howMany);
